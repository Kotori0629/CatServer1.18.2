package catserver;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class CatServer {
    public static Logger LOGGER = LogManager.getLogger("CatServer");
}
